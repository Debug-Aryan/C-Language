#include<stdio.h>
int main()
{
    printf("\n 1.Variables can start from an alphabet and underscore_");
    printf("\n 2.Special characters except _ are not allowed");
    printf("\n 3.Some particular keywords(32) are not allowed");
    printf("\n 4.Commas or blanks are not allowed");
    printf("\n 5.Variable name can be as a logical name");

    return 0;
}